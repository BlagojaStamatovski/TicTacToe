spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./db/datastore;DB_CLOSE_ON_EXIT=FALSE
    data-username: default
    data-password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    
